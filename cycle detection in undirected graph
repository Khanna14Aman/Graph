#include<iostream>
#include<bits/stdc++.h>
#define int long long 
using namespace std;
bool detectcycle(int node, int par,vector<int>adj[],vector<int>&vis)
{
    vis[node]=1;
    for(auto it: adj[node])
    {
        if(!vis[it])
        {
            if(detectcycle(it,node,adj,vis) == 1) return 1;
        }
        else if(it!=par) return 1;
    }
    return 0;
}
signed main()
{
    int n,m,i,u,v;
    cin>>n>>m;
    vector<int>adj[n+1];
    for(i=0;i<m;i++)
    {
        cin>>u>>v;
        adj[u].emplace_back(v);
        adj[v].emplace_back(u);
    }
    vector<int>vis(n+1,0);
    int flag=0;
    for(i=1;i<=n;i++)
    {
        if(!vis[i])
        {
            flag = flag || detectcycle(i,-1,adj,vis);
        }
    }
    if(flag)
    {
        cout<<"cycle\n";
    }
    else
    {
        cout<<"nocycluye\n";
    }
    return 0;
}
